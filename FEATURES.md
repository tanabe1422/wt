# wt-manager 機能リスト

最終更新: 2026-07-09

状態: `[x]` 済み / `[ ]` 未実装

実装完了したら `[ ]` を `[x]` に更新する。部分実装は未実装のまま残し、括弧で現状を注記する。

---

## 1. プラットフォーム & シェル

- [x] Wails ネイティブ起動・ビルド（`wails dev` / `wails build`）
- [x] ブラウザ単体開発 + モック API（`frontend/src/lib/wails.ts`）
- [x] メインレイアウト（リポジトリタブ + サイドバー + メイン）
- [x] エラーダイアログ
- [ ] サイドバー折りたたみトグル（インフラのみ / トグル UI なし）
- [ ] 手動リロードボタン
- [ ] キーボードショートカット

## 2. リポジトリ管理

- [x] 複数リポジトリのタブ表示・切替・閉じる
- [x] ディレクトリピッカーで Git リポジトリ追加（検証付き）
- [x] リポジトリ削除
- [x] 設定の永続化（`%AppData%/wt-manager/config.json`）

## 3. Worktree 管理

中核ギャップ: 一覧・選択は済み、ライフサイクル操作は未実装。

- [x] worktree 一覧表示（main / branch / path）
- [x] 変更ファイル数バッジ
- [x] worktree 選択 → 対応ブランチの自動選択
- [x] worktree 追加（`git worktree add`）
- [ ] worktree 削除（`git worktree remove` / `prune`）
- [ ] worktree ロック表示・操作（`isLocked` はパース済み・UI なし）
- [ ] bare worktree の UI 表示（`isBare` はパース済み・UI なし）

## 4. ブランチ管理

- [x] ローカル / リモートブランチのツリー表示
- [x] ahead / behind バッジ
- [x] worktree 紐づきマーク
- [x] ローカルブランチ切替（ダブルクリック / コンテキストメニュー）
- [x] リモートブランチのチェックアウト（追跡ブランチ作成）
- [x] ブランチ作成（ツールバー「ブランチ」→ ダイアログ）
- [x] ブランチ削除（右クリック・強制削除オプション付き）
- [ ] ブランチリネーム

## 5. ワーキングツリー（変更）

- [x] `git status` によるステージ済み / 未ステージ分割表示
- [x] ファイル単位・選択単位・一括の stage / unstage
- [x] マルチ選択
- [ ] ファイル全体の discard
- [ ] 未追跡ファイルの削除

## 6. Diff & ステージング（粒度）

- [x] 統一 diff 表示
- [x] hunk 単位の stage / unstage / discard
- [ ] バイナリ diff（現状はエラー返却）
- [ ] シンタックスハイライト

## 7. コミット & リモート

- [x] コミットメッセージ入力 + コミット
- [x] Fetch
- [x] Pull
- [x] Push（ahead / behind バッジ付きツールバー）
- [ ] upstream 設定 UI
- [ ] コンフリクト解決 UI（衝突表示 + `git mergetool` 起動は済み / アプリ内編集・ours/theirs は未実装）

## 8. 履歴 & グラフ

- [x] 全ブランチ / 現在ブランチのスコープ切替
- [x] ページネーション + 無限スクロール
- [x] カスタム SVG コミットグラフ
- [x] リサイズ可能列（メッセージ・ラベル・日付・作者・SHA）
- [x] ブランチ / タグラベル表示
- [x] 行選択ハイライト
- [x] コミット詳細ペイン（全文メッセージ・親 SHA・変更ファイル一覧）
- [x] コミットまでのリセット（soft / mixed / hard）
- [ ] コミットからの操作（checkout / revert 等）

## 9. 高度な Git 操作（将来候補）

- [x] マージ（通常 / スカッシュ）
- [ ] リベース
- [ ] cherry-pick
- [ ] stash
- [ ] worktree 間のファイル比較

## 10. UI / UX & 設定

- [x] デザイントークン（db-gui 準拠の CSS 変数）
- [x] Storybook カタログ
- [ ] 設定画面（リモート URL、デフォルトブランチ、mergetool 設定 等）

## 11. 品質 & 開発体験

- [x] Go ユニットテスト（git パッケージ）
- [x] フロント Vitest（一部）
- [x] sample-repo フィクスチャ
- [ ] E2E / 統合テスト
- [ ] CI
